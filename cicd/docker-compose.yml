version: '3.1'
services:
  postgres:
    image: postgres
    restart: always
    volumes:
      - ./postgres-data:/var/lib/postgresql/data
    ports:
      - 5432:5432
#      user postgres, db postgres, ww empty


