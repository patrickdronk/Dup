generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model domain_event {
  eventidentifier     Int    @id
  aggregateidentifier String
  eventsequencenumber Int
  payload             String
  payload_type        String
  timestamp           String
}
